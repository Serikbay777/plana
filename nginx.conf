# Plana — reverse proxy (HTTP only, без домена).

server {
    listen 80;
    server_name _;

    client_max_body_size 20m;

    # Engine API
    location /api/ {
        proxy_pass http://engine:8001/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # 900s = 15 минут — нужно для /generate/album-images, который
        # параллельно генерит 14+ gpt-image PNG. Один обычный запрос
        # gpt-image — 30-60с, но полный альбом может занять 7-12 минут.
        proxy_read_timeout 900s;
        proxy_send_timeout 900s;
    }

    # Next.js
    location / {
        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
